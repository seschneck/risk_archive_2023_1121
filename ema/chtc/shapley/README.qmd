This folder contains development code for calculating shapley values on CTHC.  Not
needed if the models are XGBoost because SHAP is quick for XGBoost.  This code was
NOT use for the ema paper but is retained for later use if needed
